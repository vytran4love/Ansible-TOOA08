---
plugin: aws_ec2
regions:
  - ap-southeast-1
filters:
  tag:Environment: prod
  instance-state-name: running
groups:
  web_group: "tags.Name == 'web'"
  api_group: "tags.Name == 'api'"
compose:
  ansible_host: private_ip_address

